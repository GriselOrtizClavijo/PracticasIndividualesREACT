import React from 'react'
import Productos, { productos } from "./Productos";

const ListComponent = () => {

   

  return (
    <div>
    <div>Lista de la compra</div>
    <div>
      <li> (product.map(product)
        (Productos key={newProduct.id} productAdd = {newProduct} )) </li>
    </div>
    <button onClick={changeAmount}>Añadir elemento</button>
    <button>Modificar elemento</button>
    </div>
  )
}

export default ListComponent